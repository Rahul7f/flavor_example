import 'package:flutter/material.dart';

class HomeProduction extends StatelessWidget {
  const HomeProduction({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
    );
  }
}
